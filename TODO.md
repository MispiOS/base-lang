# TODO
- better regex
- finish the base keywords implementation 
    - Done
        - if
        - else
        - else-if
        - while
        - function (keyword)
        - public
        - private
        - switch
        - case
        - return
        - operateurs ( = + - * / % ** && || & | ! ^ << >> == < > <= >= != !< !> )

    - todo 
        - break
        - array
        - 

# FIXME

# REDO


# DONE